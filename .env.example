# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/bc_solar_platform

# Google Gemini AI Configuration (Required for AI features)
GEMINI_API_KEY=your_gemini_api_key_here

# Application Environment
NODE_ENV=development

# PostgreSQL Connection Details (Auto-populated when using managed database)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=bc_solar_platform

# Optional: Google APIs (Not currently required due to BC regional system)
# GOOGLE_SOLAR_API_KEY=your_google_solar_api_key
# VITE_GOOGLE_SOLAR_API_KEY=your_google_solar_api_key_for_frontend